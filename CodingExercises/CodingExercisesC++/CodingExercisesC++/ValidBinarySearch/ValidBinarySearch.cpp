#include <iostream>

namespace ValidBinarySearch
{
    // 
    int main()
    {
        // Exercise: You are given a binary search and you want to test whether or note
        // a binary tree is valid or not. A valid binary search tree is a tree to which
        // all left child nodes of a given node are less than it and all right child
        // nodes are greater than it.
        std::cout << "__________________________________________________________\n";
        std::cout << "Session 2 - Valid Binary Search\n";


        std::cout << "__________________________________________________________\n";
        return 0;
    }
}
